services:
  backend:
    build: .
    ports:
      - "8013:8013"
    environment:
      - DATA_DIR=/app/data
      - WEB_DIR=/app/web
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ../data:/app/data:ro
      - ../web:/app/web:ro
