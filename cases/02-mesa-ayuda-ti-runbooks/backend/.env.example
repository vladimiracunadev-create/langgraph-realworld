# Caso 02 - Mesa de Ayuda TI / Runbooks
# Demo by default. Add real credentials only on a trusted workstation.
# Never commit a populated .env.
OPENAI_API_KEY=
# ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

# Optional hardening if you expose the backend outside localhost.
# DEMO_AUTH_TOKEN=replace-with-a-long-random-token
# RATE_LIMIT_RPM=60
# TRUST_PROXY_HEADERS=false
