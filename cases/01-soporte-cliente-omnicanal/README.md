# Caso 01: Soporte cliente omnicanal

> [!IMPORTANT]
> **Estado**: Operational | **Version**: 3.8.0 | **Referencia**: Soporte omnicanal DEMO/LIVE

- Formulario de APIs accesible desde la interfaz para exportar el `.env` del caso.
- Validacion de `ticket_id` y `thread_id` en endpoints operativos.
- Si expones el backend fuera de `localhost`, puedes activar `DEMO_AUTH_TOKEN` y `RATE_LIMIT_RPM` sin romper la demo local.
