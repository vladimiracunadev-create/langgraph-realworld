services:
  portal:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "127.0.0.1:8080:8080"
    command: python -m http.server 8080

  case01:
    build:
      context: cases/01-soporte-cliente-omnicanal/backend
    ports:
      - "127.0.0.1:8001:8001"
    volumes:
      - ./cases/01-soporte-cliente-omnicanal/data:/app/data:ro
      - ./cases/01-soporte-cliente-omnicanal/backend/web:/app/web:ro
    environment:
      - DATA_DIR=/app/data
      - WEB_DIR=/app/web
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o-mini}
      - USE_LLM=${USE_LLM:-true}

  case02:
    build:
      context: cases/02-mesa-ayuda-ti-runbooks/backend
    ports:
      - "127.0.0.1:8002:8002"
    volumes:
      - ./cases/02-mesa-ayuda-ti-runbooks/data:/app/data:ro
      - ./cases/02-mesa-ayuda-ti-runbooks/backend/web:/app/web:ro
    environment:
      - DATA_DIR=/app/data
      - OPENAI_API_KEY=${OPENAI_API_KEY}

  case09:
    build:
      context: cases/09-rrhh-screening-agenda/backend
    ports:
      - "127.0.0.1:8009:8009"
    volumes:
      - ./cases/09-rrhh-screening-agenda/data:/app/data:ro
    environment:
      - DATA_DIR=/app/data
      - OPENAI_API_KEY=${OPENAI_API_KEY}

  case10:
    build:
      context: cases/10-onboarding-empleados/backend
    ports:
      - "127.0.0.1:8010:8010"
    volumes:
      - ./cases/10-onboarding-empleados/data:/app/data:ro
    environment:
      - DATA_DIR=/app/data
      - OPENAI_API_KEY=${OPENAI_API_KEY}

  case13:
    build:
      context: cases/13-bi-analista-datos/backend
    ports:
      - "127.0.0.1:8013:8013"
    volumes:
      - ./cases/13-bi-analista-datos/data:/app/data:ro
      - ./cases/13-bi-analista-datos/web:/app/web:ro
    environment:
      - DATA_DIR=/app/data
      - OPENAI_API_KEY=${OPENAI_API_KEY}

  case03:
    build:
      context: cases/03-incident-response-sre/backend
    ports:
      - "127.0.0.1:8003:8003"
    volumes:
      - ./cases/03-incident-response-sre/data:/app/data:ro
    environment:
      - DATA_DIR=/app/data
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}

  case19:
    build:
      context: cases/19-devex-pr-review/backend
    ports:
      - "127.0.0.1:8019:8019"
    volumes:
      - ./cases/19-devex-pr-review/data:/app/data:ro
    environment:
      - DATA_DIR=/app/data
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}

  case25:
    build:
      context: cases/25-supervisor-workers/backend
    ports:
      - "127.0.0.1:8025:8025"
    volumes:
      - ./cases/25-supervisor-workers/data:/app/data:ro
    environment:
      - DATA_DIR=/app/data
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}

  # Scaffold demos — static HTML served by nginx
  case04:
    build:
      context: cases/04-soc-triage-alertas/demo
    ports:
      - "127.0.0.1:9004:80"

  case05:
    build:
      context: cases/05-analista-documentos/demo
    ports:
      - "127.0.0.1:9005:80"

  case06:
    build:
      context: cases/06-compliance-auditorias/backend
    ports:
      - "127.0.0.1:8006:8006"
    environment:
      - DATA_DIR=/app/data
      - WEB_DIR=/app/web
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - ./cases/06-compliance-auditorias/data:/app/data:ro

  case07:
    build:
      context: cases/07-compras-abastecimiento/backend
    ports:
      - "127.0.0.1:8007:8007"
    volumes:
      - ./cases/07-compras-abastecimiento/data:/app/data:ro
      - ./cases/07-compras-abastecimiento/backend/web:/app/web:ro
    environment:
      - DATA_DIR=/app/data
      - WEB_DIR=/app/web
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}

  case08:
    build:
      context: cases/08-ventas-b2b-crm/backend
    ports:
      - "127.0.0.1:8008:8008"
    volumes:
      - ./cases/08-ventas-b2b-crm/data:/app/data:ro
      - ./cases/08-ventas-b2b-crm/backend/web:/app/web:ro
    environment:
      - DATA_DIR=/app/data
      - WEB_DIR=/app/web
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}

  case11:
    build:
      context: cases/11-educacion-tutor-adaptativo/backend
    ports:
      - "127.0.0.1:8011:8011"
    volumes:
      - ./cases/11-educacion-tutor-adaptativo/data:/app/data:ro
      - ./cases/11-educacion-tutor-adaptativo/backend/web:/app/web:ro
    environment:
      - DATA_DIR=/app/data
      - WEB_DIR=/app/web
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}

  case12:
    build:
      context: cases/12-psicometria-evaluaciones/backend
    ports:
      - "127.0.0.1:8012:8012"
    volumes:
      - ./cases/12-psicometria-evaluaciones/data:/app/data:ro
      - ./cases/12-psicometria-evaluaciones/backend/web:/app/web:ro
    environment:
      - DATA_DIR=/app/data
      - WEB_DIR=/app/web
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}

  case14:
    build:
      context: cases/14-finanzas-conciliacion/backend
    ports:
      - "127.0.0.1:8014:8014"
    volumes:
      - ./cases/14-finanzas-conciliacion/data:/app/data:ro
      - ./cases/14-finanzas-conciliacion/backend/web:/app/web:ro
    environment:
      - DATA_DIR=/app/data
      - WEB_DIR=/app/web
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}

  case15:
    build:
      context: cases/15-ecommerce-postventa/backend
    ports:
      - "127.0.0.1:8015:8015"
    volumes:
      - ./cases/15-ecommerce-postventa/data:/app/data:ro
      - ./cases/15-ecommerce-postventa/backend/web:/app/web:ro
    environment:
      - DATA_DIR=/app/data
      - WEB_DIR=/app/web
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}

  case16:
    build:
      context: cases/16-viajes-planificador/demo
    ports:
      - "127.0.0.1:9016:80"

  case17:
    build:
      context: cases/17-legal-intake/backend
    ports:
      - "127.0.0.1:8017:8017"
    volumes:
      - ./cases/17-legal-intake/data:/app/data:ro
      - ./cases/17-legal-intake/backend/web:/app/web:ro
    environment:
      - DATA_DIR=/app/data
      - WEB_DIR=/app/web
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}

  case18:
    build:
      context: cases/18-marketing-contenido-qa/backend
    ports:
      - "127.0.0.1:8018:8018"
    environment:
      - DATA_DIR=/app/data
      - WEB_DIR=/app/web
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - ./cases/18-marketing-contenido-qa/data:/app/data:ro
      - ./cases/18-marketing-contenido-qa/backend/web:/app/web:ro

  case20:
    build:
      context: cases/20-migracion-legacy/demo
    ports:
      - "127.0.0.1:9020:80"

  case21:
    build:
      context: cases/21-docs-auto/backend
    ports:
      - "127.0.0.1:8021:8021"
    environment:
      - DATA_DIR=/app/data
      - WEB_DIR=/app/web
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - ./cases/21-docs-auto/data:/app/data:ro

  case22:
    build:
      context: cases/22-backoffice-automatizacion/backend
    ports:
      - "127.0.0.1:8022:8022"
    environment:
      - DATA_DIR=/app/data
      - WEB_DIR=/app/web
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - ./cases/22-backoffice-automatizacion/data:/app/data:ro
      - ./cases/22-backoffice-automatizacion/backend/web:/app/web:ro

  case23:
    build:
      context: cases/23-salud-pretriage/demo
    ports:
      - "127.0.0.1:9023:80"

  case24:
    build:
      context: cases/24-pm-assistant/demo
    ports:
      - "127.0.0.1:9024:80"
